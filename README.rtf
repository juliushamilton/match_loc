match_loc.py checks for matches of a list of DNA sequences in a reference genome. 
To use match_loc, simply navigate to the directory where it is stored in the command line, and command 
'python3 match_loc.py' with the text and any number of patterns as command line arguments. 
The text and patterns can be FASTA files or entered as strings.

match_loc builds a graph out of the patterns and runs the text over them to check for matches, 
storing match locations are returning an array.

CBO0623 is a gene in the clostridium botulinum genome, specifically an AraC family transcriptional regulator.
